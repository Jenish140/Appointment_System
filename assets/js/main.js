// /appointment_system/assets/js/main.js
console.log("Appointment System JS Loaded");

// You can add interactive elements here, for example:
// - A confirmation dialog before deleting a teacher
// - AJAX form submissions so the page doesn't reload
// - Toggling visibility of appointment booking forms